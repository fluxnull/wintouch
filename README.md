# wintouch
Windows-native touch.exe for creating files and precisely controlling Created, Modified, and Accessed timestamps from CMD. Supports exact dates, timestamp sync from reference files, relative offsets, wildcards, directories, Unicode and long paths, reparse handling, parent creation, no-create mode, and UAC relaunch.
